# Dependency Management

- Check existing dependencies before installing new ones — avoid duplicates
- Prefer built-in Node.js APIs over third-party packages when feasible
- Run `npm audit` after installing new packages
- Never install packages globally — use local project dependencies
- Pin major versions to avoid breaking changes
